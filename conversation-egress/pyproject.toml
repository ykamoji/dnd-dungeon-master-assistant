[project]
name = "conversation-egress"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymongo>=4.17.0",
    "python-dotenv>=1.2.2",
]
